# Streamlit Community Cloud / Linux builders:
# - Install CPU-only PyTorch from PyTorch's CPU index (avoids CUDA + triton on small builders).
# - Pin must match wheels for the builder Python. On Python 3.14, older pins (e.g. 2.5.1+cpu)
#   may not exist; use a version pip lists for your ABI (see deploy log), e.g. 2.11.0+cpu.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.11.0+cpu

pandas>=2.0,<3
pypdf>=4.0
scikit-learn>=1.3
numpy>=1.26,<3
faiss-cpu>=1.8
sentence-transformers>=3.0
transformers>=4.40
streamlit>=1.28
