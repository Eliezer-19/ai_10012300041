# Streamlit Community Cloud / Linux builders:
# Default `torch` from PyPI pulls CUDA stacks (nvidia-*, triton). Triton often has no wheel for
# the builder Python/ABI (e.g. Python 3.14), which fails install. Install CPU-only PyTorch first.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu

pandas>=2.0,<3
pypdf>=4.0
scikit-learn>=1.3
numpy>=1.26,<3
faiss-cpu>=1.8
sentence-transformers>=3.0
transformers>=4.40
streamlit>=1.28
